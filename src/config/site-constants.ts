import { RUNTIME_BRANDS } from "@/config/runtime-brand";

// SITE_CONFIG remains BCN-specific until the later brand-aware UI phases.
export const CANONICAL_SITE_URL = RUNTIME_BRANDS.bcn.canonicalOrigin;
export const CANONICAL_SITE_HOSTNAME = RUNTIME_BRANDS.bcn.canonicalHostname;
