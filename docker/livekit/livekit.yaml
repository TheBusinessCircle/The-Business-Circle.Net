port: 7880
bind_addresses:
  - 0.0.0.0
log_level: info

rtc:
  tcp_port: 7881
  port_range_start: 40000
  port_range_end: 40100
  use_external_ip: false

redis:
  address: redis:6379

keys:
  livekitdev: replace-me

# Embedded TURN is intentionally disabled in this repository because coturn
# is deployed as a separate service.
turn:
  enabled: false
